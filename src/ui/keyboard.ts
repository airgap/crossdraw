import { initShortcuts } from '@/ui/shortcut-registry'

/**
 * Initialize keyboard shortcuts.
 * Uses the shortcut registry which supports customization.
 */
export function setupKeyboardShortcuts() {
  initShortcuts()
}
